# Contributing

Branch from `main`, name it `feature/<slug>` or `fix/<slug>`. Run `npm run test:ci` before opening a PR — all tests must pass. Use [Conventional Commits](https://www.conventionalcommits.org/) for commit messages (e.g. `fix:`, `feat:`, `chore:`, `docs:`). PRs are squash-merged into `main`.
